# Sprint 375 Report — F640: @hono/zod-openapi 0.18.4 본 통합

**날짜**: 2026-05-10  
**Sprint**: 375  
**F-item**: F640  
**PR**: #793  
**Match Rate**: 100%

---

## 목표

F639 PoC(Sprint 374, fx-modules 단독)에서 검증된 `@hono/zod-openapi` 0.18.4 업그레이드 패턴을 나머지 4개 패키지에 적용.  
S345 cascading lock drift(F638 revert 원인) 재발 방지 게이트 포함.

---

## 구현 요약

### 업그레이드 범위

| 패키지 | 변경 | 핸들러 수정 |
|--------|------|-----------|
| packages/api | 0.9.0 → 0.18.4 | 20 route files, 40+ c.json() fixes |
| packages/fx-agent | 0.9.0 → 0.18.4 | agent.ts 52 + workflow.ts 7 = 59 handlers |
| packages/fx-offering | 0.9.0 → 0.18.4 | typecheck clean (no fixes needed) |
| packages/fx-shaping | 0.9.0 → 0.18.4 | typecheck clean (no fixes needed) |

### Breaking Change 대응 패턴 2종

1. **`c.json(result)` → `c.json(result, 200)`**: 성공 반환에 status code 누락된 케이스 (api 30+ 건)
2. **`async (c): Promise<any>`**: 반환 타입이 route schema와 구조적으로 불일치 — `ExternalReviewResult`(`@foundry-x/shared` 미빌드 시 타입 손실), 복합 union 반환 케이스 (mcp, event-status, requirements, github, fx-agent 전체)

### Lock Drift 방지 (S345 교훈)

- `pnpm install` → lock에 단일 `0.18.4(hono@4.12.8)(zod@3.25.76)` 레코드만 존재 (5 packages 일치)
- `pnpm install --frozen-lockfile` PASS — fresh CI 환경과 동일 결과 보장
- `pnpm list` 실측: `@hono/zod-openapi 0.18.4` 단일 출력

### P-f 게이트 (smoke probe 강화)

`scripts/smoke-test.sh`에 `check_http_json` 함수 추가 + 4-case multi-input login probe:
1. plain POST no body → 400|401|422
2. empty JSON `{}` → 400
3. partial body `{"email":"test@test.com"}` → 400
4. malformed JSON `{invalid}` → 400|422

---

## 검증 결과

| Gate | 결과 |
|------|------|
| typecheck `turbo --force` 19/19 | ✅ PASS (TS2345 0건) |
| test `turbo --force` | ✅ 2399/2401 PASS |
| lint | ✅ PASS |
| `pnpm install --frozen-lockfile` | ✅ PASS |
| `pnpm list` single 0.18.4 resolve | ✅ PASS |
| P-g regression tests | ✅ 4 cases (auth.test.ts:182~228) |
| P-f smoke multi-input | ✅ 4 cases added |
| Gap Analysis | ✅ Match Rate 100% |

---

## 학습 / 회고

### F638 (S345) 패턴 재발 방지 확인

S345 cascading lock drift의 근본 원인: autopilot이 package.json 5개 업데이트 + `pnpm install` 실행했지만 lock이 부분 갱신되어 `@foundry-x/fx-modules`가 여전히 `0.9.10` resolve. CI fresh install에서만 발견.

F640(본 sprint)에서의 차이:
- lock drift 0건 확인: `grep "0.9\." pnpm-lock.yaml | grep "zod-openapi"` → 0건
- `pnpm install --frozen-lockfile` PASS: lock과 node_modules 완전 일치
- `pnpm list` 실측으로 runtime resolve 확인

### fx-offering, fx-shaping 무수정 완결

두 패키지는 `package.json` 버전만 변경 + lock 갱신으로 typecheck clean. 0.18.4의 strict typing이 이 패키지들에는 영향 없음 — route handler가 없거나 strict type 조건을 이미 충족.

### stale @ts-expect-error 패턴

fx-agent의 3개 `@ts-expect-error` 지시자(`getRoutingRules`, `updateRoutingRule`, `fallbackChainGet`)는 이전에 타입 에러를 suppressing하고 있었음. 우리의 `Promise<any>` 적용으로 에러가 해소되어 `@ts-expect-error` 자체가 TS2578 에러(unused directive)로 바뀜 → 제거.

---

## 다음 단계

- Production smoke 검증 (PR #793 merge + deploy 완료 후): `bash scripts/smoke-test.sh` 전체 실행
- F640 SPEC status `📋(plan)` → `✅`
- Phase 48 또는 next backlog

---

*Sprint 375 Report generated by sprint-autopilot*
